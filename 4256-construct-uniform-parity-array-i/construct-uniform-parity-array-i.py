class Solution:
    def uniformArray(self, nums1: list[int]) -> bool:
        #even-even =even,odd-odd = even and |odd-even|=odd
        #it always return True because 
        #if all are same its true
        #if atleat one is diffrent you can make the entire elements in one order
        return True
        

        